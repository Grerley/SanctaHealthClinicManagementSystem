{
  "name": "@sancta/cloud-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker: cloud API, synchronisation ingress and integration gateway (CLD-002).",
  "exports": {
    ".": "./src/cloud-sync.ts",
    "./node-adapter": "./node/adapter.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json",
    "test": "node --experimental-strip-types --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@sancta/domain": "*"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  }
}
