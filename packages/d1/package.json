{
  "name": "@sancta/d1",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "D1-shaped data layer: the D1 binding interface plus a node:sqlite-backed local driver, so the same SQL runs in Cloudflare D1 (prod) and in tests (local SQLite).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --experimental-sqlite --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "@sancta/domain": "*"
  },
  "exports": {
    ".": "./src/index.ts",
    "./migrations": "./src/migrations.ts",
    "./local": "./src/local.ts"
  }
}
